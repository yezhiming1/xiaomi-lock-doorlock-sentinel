# 验证记录

本文件只记录可公开、无真实家庭数据的证据；私有 NAS 录像、路径、域名、凭据和时间线不写入仓库。

| 层级 | 检查 | 当前结果 |
|---|---|---|
| 聚焦 | Ruff 静态检查 | PASS |
| 聚焦 | Python 单元与安全测试 | PASS，30 项；1 条仅测试依赖的弃用警告 |
| 聚焦 | Node 企业微信适配测试 | PASS，2 项 |
| 聚焦 | TypeScript 生产构建 | PASS |
| 聚焦 | Node 生产依赖高危漏洞审计 | PASS，0 项 |
| 本地回归 | Alembic upgrade/downgrade/upgrade | PASS |
| 本地回归 | 合成视频端到端烟雾链 | PASS |
| 本地回归 | 固定 SCRFD/ArcFace 模型加载 | PASS |
| 容器 | 候选镜像构建 | PASS；依赖层与源码层分离，锁定基础镜像摘要 |
| 容器 | 健康、非 root、只读输入和重启恢复 | PASS；真实锁定模型就绪，业务进程 UID 10001，可读 `root:root 0640` 输入但无法写入，输入/模型/根文件系统只读，重启后状态不重复 |
| 浏览器 | 390 / 768 / 1440 管理页面 | PASS；四路由、登录失败/成功、弹窗与键盘焦点、44px 触控、无水平溢出、AA 文字对比度 |
| 目标 NAS | 私有录像、性能与持久化 | PASS；29 条受管录像全部处理，7 个完成、22 个清晰度跳过、错误 0；数据库完整，输入集合复核不变 |
| 公网 | TLS、密码登录、既有站点无回归 | PASS；安全 Cookie、服务端会话、CSRF 退出和共享网关回归通过 |
| 企业微信 | 真实账号私聊 | UNKNOWN，凭据尚未配置 |
| 115 | 上传、校验、月封、删除与恢复 | OUT OF SCOPE，由专项任务验收 |

正式发布必须以最终提交和最终镜像身份重新执行全部适用检查，并把尚未执行项保持为 `UNKNOWN`，不得由相邻证据推断通过。

补充：Auteur `slopscan` 最终结果为 `0 fails, 0 warns, 0 suppressed`。Impeccable 静态探测器因其本地 HTML 解析模块缺失而以降级正则模式运行，并给出一条对无圆角原生弹窗的误报；该工具结果不记为完整 PASS。实际浏览器计算样式的 AA 对比度、触控目标、溢出和可见焦点检查均单独通过。

目标运行补充：生产容器限制为 2 CPU、2 GiB、128 PIDs；复核时约占 1002 MiB、24 PIDs，
根文件系统和输入挂载只读，未发生 OOM 或自动重启。目标批次平均处理约 15 秒；单个最大值
来自冷启动或模型初始化，不能直接当作稳定态单录像基准。真实文件名、时间、内容、路径和
媒体摘要不进入仓库。
