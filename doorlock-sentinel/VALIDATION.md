# 验证记录

本文件只记录可公开、无真实家庭数据的证据；私有 NAS 录像、路径、域名、凭据和时间线不写入仓库。

| 层级 | 检查 | 当前结果 |
|---|---|---|
| 聚焦 | Ruff 静态检查 | PASS |
| 聚焦 | Python 单元与安全测试 | PASS，43 项；1 条仅测试依赖的弃用警告 |
| 聚焦 | Node 企业微信适配测试 | PASS，2 项 |
| 聚焦 | TypeScript 生产构建 | PASS |
| 聚焦 | Node 生产依赖高危漏洞审计 | PASS，0 项 |
| 本地回归 | Alembic upgrade/downgrade/upgrade | PASS |
| 本地回归 | 合成视频端到端烟雾链 | PASS |
| 本地回归 | 固定 SCRFD/ArcFace 模型加载 | PASS |
| 容器 | V0.0.4 生产镜像构建 | PASS；规范 Dockerfile 解析外部元数据超时后，使用最终源码提交、已验证前代镜像 ID 和不变依赖锁完成受身份约束的离线代码层构建 |
| 容器 | 健康、非 root、只读输入和重启恢复 | PASS；真实锁定模型就绪，业务进程 UID 10001，可读 `root:root 0640` 输入但无法写入，输入/模型/根文件系统只读，重启后状态不重复 |
| 浏览器 | Edge 桌面与 390×844 管理页面 | PASS；已确认人物优先、中文运行记录、合并/撤销闭环、人物簇大图与录像辅助确认、键盘焦点、无水平溢出且控制台错误 0 |
| 目标 NAS | V0.0.4 在线快照、迁移、切换与持久化 | PASS；快照完整性、34 个数据库路径、23+23 张历史图片迁移、文件内容集合、容器健康、重启计数和只读边界均复核通过 |
| 公网 | V0.0.4 TLS、密码登录、媒体与既有站点无回归 | PASS；39 条事件可见，新命名派生图片与源录像可读取，安全会话和共享网关既有路由通过 |
| 聚焦 | V0.0.4 媒体命名与迁移 | PASS；北京时间录像解析、同秒序号、`-aNNN` / `-bNNN`、大小与 SHA-256 校验、图片无覆盖切换、数据库路径更新和重复 dry-run 归零 |
| 目标 NAS | V0.0.4 历史迁移与部署 | PASS；34 个历史数据库路径、23 张人脸图和 23 张场景图迁移，补回 5 个遗漏录像后共 39 个录像全部处理且分析失败为 0 |
| 企业微信 | 真实账号私聊 | UNKNOWN，凭据尚未配置 |
| 115 | 上传、校验、月封、删除与恢复 | OUT OF SCOPE，由专项任务验收 |

V0.0.4 规范 Dockerfile 在 NAS 解析固定 Docker Hub 基础镜像元数据时发生连接超时，因此没有
把该路径记录为 PASS，也没有原样盲重试。V0.0.3 → V0.0.4 的基础镜像与依赖锁内容不变，
随后以最终合并提交和已验证前代镜像 ID 完成受身份约束的离线代码层升级构建；断网版本复核、
生产切换、健康、只读边界、真实消费者与公网媒体读取均通过。

正式发布必须以最终提交和最终镜像身份重新执行全部适用检查，并把尚未执行项保持为 `UNKNOWN`，不得由相邻证据推断通过。

补充：Auteur `slopscan` 最终结果为 `0 fails, 0 warns, 0 suppressed`。Impeccable 静态探测器因其本地 HTML 解析模块缺失而以降级正则模式运行，并给出一条对无圆角原生弹窗的误报；该工具结果不记为完整 PASS。实际浏览器计算样式的 AA 对比度、触控目标、溢出和可见焦点检查均单独通过。

V0.0.4 目标运行补充：生产容器限制为 2 CPU、2 GiB、128 PIDs；根文件系统、输入、模型和
Secret 挂载保持只读，未发生 OOM 或自动重启。V0.0.3 可运行源码、镜像、私有配置和迁移前
数据库在线快照继续保留。真实文件名、时间、内容、路径和媒体摘要不进入仓库。
