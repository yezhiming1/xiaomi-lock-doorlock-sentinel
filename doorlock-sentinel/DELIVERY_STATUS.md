# 交付状态

版本：`V0.0.6` 候选

当前状态：`LOCAL_VERIFIED / RELEASE_PENDING / TARGET_PENDING`

已具备的聚焦证据：

- Python 静态检查通过。
- V0.0.6 在既有关系分类中加入“我”和“朋友”，不迁移数据库、不自动改写历史人物；
  “我”沿用家人风险减分，“朋友”保持中性风险语义。
- V0.0.6 Python 50 项、Node 4 项、TypeScript 构建、生产依赖审计、迁移往返、合成烟雾链、
  模型锁、119 个公开文件隐私审计和 Compose 配置通过。
- V0.0.5 将接口时间统一为明确 UTC，并把管理页面显示固定为北京时间；北京时间自然日边界、
  页面版本号和静态资源版本均有回归测试。
- V0.0.5 Python 46 项、Node 4 项、TypeScript 构建、生产依赖审计、迁移往返、合成烟雾链、
  模型锁和 119 个公开文件隐私审计通过。
- Python 单元与安全测试通过（43 项）；V0.0.4 新增北京时间解析、a/b 命名及数据库/
  文件迁移幂等测试。
- 企业微信适配层测试、TypeScript 构建和生产依赖漏洞审计通过。
- Alembic 新库升级/降级/再次升级通过。
- 合成视频到数据库、人物标记和备份回执烟雾链通过。
- 固定 InsightFace 模型包和两个 ONNX 文件通过 SHA-256 与后端加载检查。
- V0.0.4 镜像已从已验证的 V0.0.3 前代镜像和最终源码提交完成受身份约束的离线代码层构建；
  依赖与基础镜像保持不变。
- 真实锁定模型在生产模式容器中就绪；重启持久化、非 root 业务进程、`root:root 0640` 输入读取、只读根和只读输入边界通过。
- 管理页面在 Edge 的桌面与 390×844 视口完成登录、人物合并、中文运行记录、撤销、键盘焦点、触控尺寸和溢出验收；控制台错误为 0。

发布与目标验收状态：

- V0.0.6 尚未发布或部署；当前生产继续运行 V0.0.5、健康且零重启。V0.0.6 的 GitHub、
  NAS、真实入口和人物分类交互验收保持 `UNKNOWN`。
- V0.0.5 公开 Pull Request、CI、合并、生产镜像构建、数据库在线快照和无损切换完成；最终
  源码提交为 `442425065f845b3e7107bba09b7f67af4faa2d86`，生产镜像 ID 为
  `sha256:918c4de5db0534736f6f9ac9404303432db030a89fcead44ab9a8547fc4acd0c`。
- 生产容器健康、零重启且继续使用原 `bridge` 网络；39 条事件、26 条人物轨迹、40 个输入
  文件、52 个派生文件、数据库完整性、只读边界和资源上限与切换前一致。
- 公网 Edge 桌面与 390×844 视口确认 V0.0.5 标题/品牌、北京时间说明、最新事件时间一致性、
  39 条事件、既有录像消费者、无水平溢出和控制台零错误。
- V0.0.4 镜像、源码、私有配置和 V0.0.5 部署前数据库在线快照继续作为可运行回滚集保留。
- V0.0.4 公开 Pull Request、CI、合并和 Release 完成；最终源码提交为
  `6e58c2e553082eaf4b8a0df9d66b41a88b9d76df`。
- 执行前创建并独立校验 SQLite 在线快照；数据库完整性为 `ok`，且无活动租约、缺失引用、
  孤立文件或内容摘要不匹配。
- 历史迁移完成 34 个录像数据库路径、23 张人脸裁剪图和 23 张带框场景图；dry-run、
  应用和再次 dry-run 均通过，文件数量、总字节与内容摘要集合保持一致。
- V0.0.4 生产镜像 ID 为
  `sha256:71efd28797782eabe4c273526a4a9e2833ec52d3b929ae04c45d0f4124848cf9`；容器健康、
  重启计数为 0，非 root、只读根、只读输入、资源上限和启动策略保持不变。
- 部署后补回 5 个遗漏录像；当前 39 个录像全部完成识别消费、分析失败为 0。有效派生图
  各 26 张，全部使用 `-aNNN` / `-bNNN` 命名。
- 共享公网入口、密码登录、安全会话、新命名人物簇图片与源录像访问、以及全部既有站点
  无回归验证通过。
- V0.0.3 镜像、源码、私有配置和迁移前在线快照继续作为可运行回滚集保留。

持续项与外部边界：

- 企业微信真实账号验证；凭据未配置前保持 `UNKNOWN`。
- 115 真实备份与删除验收由专项任务完成，本项目不得替代其结论。
- 实际身份准确率依赖后续人工标记，训练持续进行且当前不宣称通过。

不能把 NAS、公网证据扩大为企业微信、115 或真实身份准确率结论。
