# 交付状态

版本：`V0.0.2`

当前状态：`RELEASE CANDIDATE / TRAINING`

已具备的聚焦证据：

- Python 静态检查通过。
- Python 单元与安全测试通过。
- 企业微信适配层测试、TypeScript 构建和生产依赖漏洞审计通过。
- Alembic 新库升级/降级/再次升级通过。
- 合成视频到数据库、人物标记和备份回执烟雾链通过。
- 固定 InsightFace 模型包和两个 ONNX 文件通过 SHA-256 与后端加载检查。
- 最终候选 Docker 镜像已从锁定依赖和当前源码构建。
- 真实锁定模型在生产模式容器中就绪；重启持久化、非 root 业务进程、`root:root 0640` 输入读取、只读根和只读输入边界通过。
- 管理页面在 Edge 的 390/768/1440 视口完成四路由、登录、弹窗、键盘焦点、触控尺寸、溢出与 AA 对比度验收。

已完成发布与目标验收：

- 公开 Release 源码包、镜像包及哈希已发布；公开源码包完成 111 文件恢复验证，另行保留的私有首版源码恢复集完成 110 文件恢复验证。
- NAS 部署、私有录像处理、输入集合不变和资源上限复核。
- 共享公网入口旁路发布、密码登录、安全会话和既有站点无回归验证。

持续项与外部边界：

- 企业微信真实账号验证；凭据未配置前保持 `UNKNOWN`。
- 115 真实备份与删除验收由专项任务完成，本项目不得替代其结论。
- 实际身份准确率依赖后续人工标记，训练持续进行且当前不宣称通过。

不能把 NAS、公网证据扩大为企业微信、115 或真实身份准确率结论。
