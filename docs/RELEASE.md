# 发布记录

## V0.0.3

状态：`RELEASED / DEPLOYED`

本版本将运行记录的操作与对象改为可读中文，并把“并入已确认人物”纳入正式领域操作、审计和撤销链；待确认人物的合并目标优先展示已确认人物。功能实现提交为 `3a5c376b15b5e8529909ec78e29a6016200fc29f`。

发布与目标证据：

- Ruff、Python 40 项、Node 2 项、TypeScript 构建、生产依赖审计、迁移往返、合成烟雾链、模型锁和公开目录审计通过。
- Edge 桌面与 390×844 交互通过；合并列表默认聚焦并选择“已确认人物（优先）”，合并、中文运行记录和撤销恢复闭环通过，控制台错误为 0。
- NAS 隔离侧车、无损切换和受控重启通过；数据库聚合与只读源录像集合在升级和重启前后不变，输入与根文件系统写入探测均被拒绝。
- 公网 TLS、密码登录、安全 Cookie、V0.0.3 bootstrap、人物簇媒体字段、中文操作对象、CSRF 退出、会话撤销和共享网关全部既有路由通过。
- Linux/AMD64 image config 为 `sha256:f78b216268d5654fea402f85dab149767e862a9a3f95542781640e4e6c7c125c`；V0.0.2 完整源码、镜像、模型和上线前 SQLite 在线快照继续作为可运行回滚集保留。
- 镜像归档、私有模型恢复副本、源码包、SHA-256 和独立恢复验证只在私有发布目录封存；公开 Release 不含模型或家庭数据。

企业微信真实账号、115 实际备份和真实身份准确率的证据边界不变，不能由本版本推断通过。

## V0.0.2

状态：`RELEASED / DEPLOYED`

本版本新增人物名称留空后按关系顺延编号，以及待确认人物簇的大图、样本切换和对应录像按需播放。实现提交为 `1507aa98019cba63196d00a006b4cc5f77d545f0`。

发布与目标证据：

- Ruff、Python 36 项、Node 2 项、TypeScript 构建、生产依赖审计、迁移往返、合成烟雾链、模型锁和公开目录审计通过。
- Edge 桌面与 390×844 交互通过；录像默认不自动播放、`preload=none`，点击后可完整播放，样本切换保留键盘焦点。
- NAS 无损升级与受控重启通过；数据库、人物/原型聚合及 29 条源录像的数量、大小和内容集合在切换前后不变。
- 公网 TLS、密码登录、安全 Cookie、V0.0.2 bootstrap、人物簇媒体字段、CSRF 退出、会话撤销和共享网关全部既有路由通过。
- V0.0.1 完整源码、镜像、模型和数据库快照回滚集继续保留；V0.0.2 上线前另生成并校验 SQLite 在线快照。
- 镜像归档与模型私有恢复副本的 SHA-256、平台清单和恢复验证记录只保存在 Git 忽略的私有发布目录；公开 Release 不含模型或家庭数据。

企业微信真实账号、115 实际备份和真实身份准确率的证据边界不变，不能由本版本推断通过。

## V0.0.1

状态：`RELEASED / DEPLOYED`

正式发布包含：

- 公开源码仓库的干净首个提交与 tag `v0.0.1`。
- 不含模型、录像、人脸、向量、数据库、secret、私有地址和旧参考 ZIP 的源码归档。
- `doorlock-sentinel:0.0.1` 镜像归档。
- 文件清单和 SHA-256 校验文件。
- 本地、容器、浏览器、NAS、公网和外部账号分层验证结果。
- 数据库快照、上一可运行代、共享入口回滚和镜像恢复步骤。

## 已完成证据

- 生产实现与镜像来源提交：`153f70ad694ba23af9ba62cbad116658be542fa4`；公开 GitHub tag `v0.0.1` 指向 `9b8e67cba2085ba840c42fa2343308dbe7e84813`，公开源码包完成 111 文件恢复验证。
- Ruff、Python 30 项、Node 2 项、TypeScript 构建、生产依赖高危漏洞审计、迁移往返、
  合成烟雾链、固定模型加载和容器运行契约均通过。
- 公开 Release 源码包 SHA-256：`0842fb268428a800dafc4be2a6bdf3487d0c3452c88adf4202817d98284acb66`；
  另行保留的 110 文件私有首版源码恢复包 SHA-256 为 `9d9578d907d4f6bbc84219ddc0c3bf777a984e4f540e12d71b788fba7351e13c`。
- 镜像归档 SHA-256：`d6c0cc5744e8517168485f2c1a617c341170b0e9fcfe12e03a44cdb5eaf14fc6`；
  目标镜像配置摘要为 `sha256:9c6a1661ff677abf4b5c53f18090b19a67b93a7586ed812cc0f95cb9ac0b9a1b`。
- 私有模型恢复包已保存并校验，但因权重许可边界不进入 Git 或公开 Release。
- 目标 NAS 生产容器健康，私有录像批次全部处理且分析错误为 0，输入集合复核前后不变。
- 公网 TLS、密码登录、安全 Cookie、服务端会话、CSRF 退出及共享网关既有路由回归通过。

## 仍保持未知或外部范围

- 企业微信真实账号未配置，真实发送保持 `UNKNOWN`。
- 实际人物准确率需后续人工标记校准，当前不宣称通过。
- 115 上传、校验、月度封存、删除和恢复由专项任务负责，本项目不宣称完成。

公开 Release 不包含模型、录像、人脸、向量、数据库、Secret、私有地址或真实家庭时间线。
