# 发布记录

## V0.0.6

状态：`RELEASED / DEPLOYED`

本版本在既有“与您家的关系”中加入“我”和“朋友”，覆盖人物确认、修改、卡片、事件详情
和合并选择器。历史人物不自动改写；名称留空时继续按关系顺延编号。“我”沿用家人的风险
减分，“朋友”保持已知人物的中性风险语义。没有新增数据库字段或迁移。

发布与目标证据：

- Ruff、Python 50 项、Node 4 项、TypeScript 构建、生产依赖审计、迁移往返、合成烟雾链、
  模型锁、119 个公开文件隐私审计和 Compose 配置在本地通过。
- 公开 Pull Request #11、CI、合并、tag `v0.0.6` 和 GitHub Release 通过；最终源码提交为
  `b27e964d21cb01cc2c41fbe4d122169398e886c5`。
- 基础镜像、依赖锁和 Node 运行代码未变化，使用已验证的 V0.0.5 镜像完成受身份约束的
  离线代码层构建；生产镜像 ID 为
  `sha256:1330103c4b5002918d91cc993f3f04294566d375a7c552bdee80473006ebbb94`。
- NAS 隔离候选的数据库、模型、只读根、资源限制、版本、分类选项与源码提交身份通过；
  Edge 桌面与 390×844 视口验证人物确认、修改、卡片、事件详情、合并选择器、自动命名提示、
  键盘顺序、44px 选择器、54px 手机导航、无水平溢出和应用控制台零错误，清理后残留为 0。
- 发布边界为 `gate_applicability = NON_INSTALLER_RELEASE`；本项目继续交付 Docker 镜像和公开
  源码，不交付安装器。
- V0.0.5 的 tag 与公开 Release 指向提交 `442425065f845b3e7107bba09b7f67af4faa2d86`；
  前代镜像身份、镜像归档、私有配置和 SQLite 在线快照已重新校验，快照独立完整性为
  `ok`。最初两次备份尝试因权限边界在数据库目标打开前失败并保留证据，第三次使用隔离
  暂存路径完成，未中断生产。
- 切换时重新生成并独立校验在线快照与完整恢复清单。旧容器引用的临时 Compose 覆盖文件
  已缺失，未继续依赖该路径；改用最终源码目录中的持久化环境和 NAS 覆盖配置，规范化校验
  后沿用原 `bridge` 网络完成无损切换。
- 部署后容器健康、重启计数为 0；数据库完整性与外键通过，全部业务表、输入录像和派生
  文件集合与切换前一致。启动按既有契约清理 3 条过期登录会话，不属于业务数据损失。
- 公网健康、TLS 与现有密码会话通过；Edge 桌面和 390×844 视口确认 V0.0.6、北京时间、
  “我/朋友”分类、真实事件与媒体消费者、无水平溢出和应用控制台零错误。共享入口未改动。

回滚边界：继续保留 V0.0.5 镜像、源码、私有配置、上线前 SQLite 快照、镜像归档和摘要
清单；恢复后必须验证容器
健康、重启计数为 0、数据库完整、只读输入与资源边界不变，并由真实管理页面复核人物和
媒体消费者。

## V0.0.5

状态：`RELEASED / DEPLOYED`

本版本修正管理页面时间契约：服务端统一输出明确 UTC，客户端固定按 `Asia/Shanghai`
显示，并按北京时间自然日判断“今天/昨天”；浏览器标题、登录页和品牌区显示
“门锁观察簿 V0.0.5”。最终源码提交为
`442425065f845b3e7107bba09b7f67af4faa2d86`。

发布与目标证据：

- Ruff、Python 46 项、Node 4 项、TypeScript 构建、生产依赖审计、迁移往返、合成烟雾、
  模型锁、119 个公开文件隐私审计、公开 Pull Request 与 CI 均通过。
- 依赖与基础镜像未变化，使用已验证 V0.0.4 前代镜像和最终提交完成身份约束的离线代码层
  构建；生产镜像 ID 为
  `sha256:918c4de5db0534736f6f9ac9404303432db030a89fcead44ab9a8547fc4acd0c`。
- 隔离候选在 Edge 中确认 V0.0.5 标题、品牌、北京时间说明和 UTC 跨日样本的 `00:30 / 今天`
  显示；候选容器健康、零重启、只读根，验收后无残留。
- 部署前 SQLite 在线快照通过独立完整性、摘要、数据聚合和零活动租约校验。首次独立 Compose
  切换因 NAS 地址池耗尽在创建网络前停止，生产 V0.0.4 未被替换且自动回滚复读通过；随后
  沿用原完整 Compose 叠加配置和 `bridge` 网络，仅覆盖镜像版本完成切换。
- 部署后容器健康、重启计数为 0；39 条事件、26 条人物轨迹、40 个输入文件、52 个派生文件
  及数据库完整性与切换前一致，根文件系统、输入、模型和 Secret 只读边界保持不变。
- 公网 Edge 桌面与 390×844 视口确认标题和品牌为 V0.0.5、39 条事件正常、最新事件显示与
  数据库北京时间计算一致、既有录像消费者存在、无水平溢出且控制台错误为 0。

发布门禁：

- `gate_applicability = NON_INSTALLER_RELEASE`；本项目交付 Docker 镜像与公开源码，不交付
  安装器，安装器路径与安装器摘要为 `NOT_APPLICABLE`。
- 当前版本为 `0.0.5`，可运行前代为 `0.0.4`；两代镜像、源码、私有配置和部署前数据库
  在线快照均保留，V0.0.5 无数据库迁移变更。
- 回滚方法为恢复 V0.0.4 镜像与原配置；接受条件为容器健康、重启计数为 0、数据库完整、
  只读边界不变且真实管理页面与媒体消费者恢复。

企业微信真实账号、115 实际备份和真实身份准确率的证据边界不变，不能由本版本推断通过。

## V0.0.4

状态：`RELEASED / DEPLOYED`

本版本配合下载器 V0.0.8 完成北京时间录像命名与 `-aNNN` / `-bNNN` 派生图片命名，
并提供先 dry-run、再应用、再次 dry-run 归零的无覆盖数据库/文件迁移。最终源码提交为
`6e58c2e553082eaf4b8a0df9d66b41a88b9d76df`。

发布与目标证据：

- Ruff、Python 43 项、Node 2 项、TypeScript 构建、生产依赖审计、迁移往返、合成烟雾链、
  模型锁和公开目录审计通过；公开 Pull Request、CI、合并和 `v0.0.4` Release 完成。
- 规范 Dockerfile 在解析外部固定基础镜像元数据时连接超时，未把该路径记为 PASS，也未
  原样盲重试。由于基础镜像与依赖锁不变，使用最终源码提交和已验证 V0.0.3 镜像完成受
  身份约束的离线代码层构建。
- 执行前 SQLite 在线快照通过独立完整性与摘要校验；34 个录像数据库路径、23 张历史人脸
  裁剪图和 23 张历史场景图完成迁移，重复 dry-run 归零，数量、总字节和内容摘要集合不变。
- 生产镜像 ID 为
  `sha256:71efd28797782eabe4c273526a4a9e2833ec52d3b929ae04c45d0f4124848cf9`；无损切换后
  容器健康、重启计数为 0，非 root、只读根、只读挂载、资源上限与启动策略保持不变。
- 下载器补回 5 个遗漏录像后共有 39 个受管录像，全部完成识别消费，分析失败为 0；有效
  人脸裁剪图和场景图各 26 张，全部使用新命名。
- Edge 公网消费者确认事件列表、新命名人物簇图片和源录像均可用；既有共享网关路由无回归。
- V0.0.3 镜像、源码、私有配置和迁移前数据库在线快照继续作为可运行回滚集保留。

企业微信真实账号、115 实际备份和真实身份准确率的证据边界不变，不能由本版本推断通过。

## V0.0.3

状态：`RELEASED / DEPLOYED`

本版本将运行记录的操作与对象改为可读中文，并把“并入已确认人物”纳入正式领域操作、审计和撤销链；待确认人物的合并目标优先展示已确认人物。功能实现提交为 `3a5c376b15b5e8529909ec78e29a6016200fc29f`。

发布与目标证据：

- Ruff、Python 40 项、Node 2 项、TypeScript 构建、生产依赖审计、迁移往返、合成烟雾链、模型锁和公开目录审计通过。
- Edge 桌面与 390×844 交互通过；合并列表默认聚焦并选择“已确认人物（优先）”，合并、中文运行记录和撤销恢复闭环通过，控制台错误为 0。
- NAS 隔离侧车、无损切换和受控重启通过；数据库聚合与只读源录像集合在升级和重启前后不变，输入与根文件系统写入探测均被拒绝。
- 公网 TLS、密码登录、安全 Cookie、V0.0.3 bootstrap、人物簇媒体字段、中文操作对象、CSRF 退出、会话撤销和共享网关全部既有路由通过。
- Linux/AMD64 image config 为 `sha256:f78b216268d5654fea402f85dab149767e862a9a3f95542781640e4e6c7c125c`；V0.0.2 完整源码、镜像、模型和上线前 SQLite 在线快照继续作为可运行回滚集保留。
- 镜像归档、私有模型恢复副本、源码包、SHA-256 和独立恢复验证只在私有发布目录封存；公开 Release 不含模型或家庭数据。

企业微信真实账号、115 实际备份和真实身份准确率的证据边界不变，不能由本版本推断通过。

## V0.0.2

状态：`RELEASED / DEPLOYED`

本版本新增人物名称留空后按关系顺延编号，以及待确认人物簇的大图、样本切换和对应录像按需播放。实现提交为 `1507aa98019cba63196d00a006b4cc5f77d545f0`。

发布与目标证据：

- Ruff、Python 36 项、Node 2 项、TypeScript 构建、生产依赖审计、迁移往返、合成烟雾链、模型锁和公开目录审计通过。
- Edge 桌面与 390×844 交互通过；录像默认不自动播放、`preload=none`，点击后可完整播放，样本切换保留键盘焦点。
- NAS 无损升级与受控重启通过；数据库、人物/原型聚合及 29 条源录像的数量、大小和内容集合在切换前后不变。
- 公网 TLS、密码登录、安全 Cookie、V0.0.2 bootstrap、人物簇媒体字段、CSRF 退出、会话撤销和共享网关全部既有路由通过。
- V0.0.1 完整源码、镜像、模型和数据库快照回滚集继续保留；V0.0.2 上线前另生成并校验 SQLite 在线快照。
- 镜像归档与模型私有恢复副本的 SHA-256、平台清单和恢复验证记录只保存在 Git 忽略的私有发布目录；公开 Release 不含模型或家庭数据。

企业微信真实账号、115 实际备份和真实身份准确率的证据边界不变，不能由本版本推断通过。

## V0.0.1

状态：`RELEASED / DEPLOYED`

正式发布包含：

- 公开源码仓库的干净首个提交与 tag `v0.0.1`。
- 不含模型、录像、人脸、向量、数据库、secret、私有地址和旧参考 ZIP 的源码归档。
- `doorlock-sentinel:0.0.1` 镜像归档。
- 文件清单和 SHA-256 校验文件。
- 本地、容器、浏览器、NAS、公网和外部账号分层验证结果。
- 数据库快照、上一可运行代、共享入口回滚和镜像恢复步骤。

## 已完成证据

- 生产实现与镜像来源提交：`153f70ad694ba23af9ba62cbad116658be542fa4`；公开 GitHub tag `v0.0.1` 指向 `9b8e67cba2085ba840c42fa2343308dbe7e84813`，公开源码包完成 111 文件恢复验证。
- Ruff、Python 30 项、Node 2 项、TypeScript 构建、生产依赖高危漏洞审计、迁移往返、
  合成烟雾链、固定模型加载和容器运行契约均通过。
- 公开 Release 源码包 SHA-256：`0842fb268428a800dafc4be2a6bdf3487d0c3452c88adf4202817d98284acb66`；
  另行保留的 110 文件私有首版源码恢复包 SHA-256 为 `9d9578d907d4f6bbc84219ddc0c3bf777a984e4f540e12d71b788fba7351e13c`。
- 镜像归档 SHA-256：`d6c0cc5744e8517168485f2c1a617c341170b0e9fcfe12e03a44cdb5eaf14fc6`；
  目标镜像配置摘要为 `sha256:9c6a1661ff677abf4b5c53f18090b19a67b93a7586ed812cc0f95cb9ac0b9a1b`。
- 私有模型恢复包已保存并校验，但因权重许可边界不进入 Git 或公开 Release。
- 目标 NAS 生产容器健康，私有录像批次全部处理且分析错误为 0，输入集合复核前后不变。
- 公网 TLS、密码登录、安全 Cookie、服务端会话、CSRF 退出及共享网关既有路由回归通过。

## 仍保持未知或外部范围

- 企业微信真实账号未配置，真实发送保持 `UNKNOWN`。
- 实际人物准确率需后续人工标记校准，当前不宣称通过。
- 115 上传、校验、月度封存、删除和恢复由专项任务负责，本项目不宣称完成。

公开 Release 不包含模型、录像、人脸、向量、数据库、Secret、私有地址或真实家庭时间线。
