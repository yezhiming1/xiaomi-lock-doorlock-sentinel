# 当前交接

权威项目根为本目录，正式应用位于 `doorlock-sentinel/`。旧 `V0.1.0` ZIP 只在 Git 忽略的私有归档区保留，不能再作为实现来源。

## 已完成代码面

- V0.0.6 已完成“我/朋友”关系分类、默认命名、页面显示和风险语义；本地完整回归、
  GitHub Release、NAS 隔离候选、无损切换及 Edge 桌面/手机真实入口验收通过。
- V0.0.5 的明确 UTC 接口时间、北京时间页面显示、北京时间自然日判断和可见版本号已完成；
  本地完整回归、NAS 隔离候选、生产切换及 Edge 桌面/手机真实消费者验收通过。
- SCRFD/ArcFace ONNX CPU 后端与模型哈希锁。
- 只读摄取、SHA-256 去重、租约恢复与自动重试。
- 多人物轨迹、完整同帧证据和不可合并约束。
- 未知人物自然聚类、人工命名/改名/合并/拆分/误检/撤销；待确认人物可直接并入已确认人物，且同框冲突仍拒绝。
- 永久多样代表样本及有界检索索引。
- 密码管理页面、安全会话、审计与运行设置。
- 企业微信官方 SDK Outbox 适配。
- 下载器 `V0.0.4` 脱敏状态日志桥接。
- 115 制品清单、数据库快照与回执协议。

## 继续顺序

1. 持续通过管理页面人工标记人物。
2. 依据人工标记数据校准阈值；在证据不足前不启用身份或风险通知。
3. 企业微信账号由用户私下配置后，再完成真实故障消息验收。
4. 115 上传、远端校验、月度封存、删除和恢复继续由网盘备份专项任务负责。
5. 下载器每日计划周期、断网和空间不足仍由其项目单独观察，不能由本项目代替。

## 不得越权

- 不从聊天、HA 或下载器导出云端凭据。
- 不启用真实企微，直到用户在 NAS 私下配置账号。
- 不由本项目上传或删除 115 数据。
- 不把本地测试、容器健康或公网登录互相替代。

## 已完成交付

- `V0.0.6` 已发布并部署；最终源码提交为
  `b27e964d21cb01cc2c41fbe4d122169398e886c5`，生产镜像 ID 为
  `sha256:1330103c4b5002918d91cc993f3f04294566d375a7c552bdee80473006ebbb94`。
- 上线前 SQLite 在线快照、V0.0.5 镜像归档、私有配置和恢复清单均通过摘要与独立完整性
  校验；切换后业务表、输入录像和派生文件集合一致，仅按启动契约清理 3 条过期登录会话。
- Edge 桌面与 390×844 真实入口确认 V0.0.6 标题、人物分类、自动命名提示、北京时间、
  既有录像消费者、无水平溢出和应用控制台零错误；共享网关未改动。
- `V0.0.5` 已发布并部署；生产镜像绑定提交
  `442425065f845b3e7107bba09b7f67af4faa2d86`，V0.0.4 镜像、源码、私有配置和部署前
  数据库在线快照继续作为可运行回滚集保留。
- 管理页面所有时间固定按北京时间显示；V0.0.5 公网
  Edge 桌面与 390×844 视口确认 39 条事件、既有录像消费者、无水平溢出和控制台零错误。
- `V0.0.4` 已发布并部署；迁移前数据库在线快照通过独立校验，V0.0.3 镜像、源码、
  私有配置和快照继续作为可运行回滚集保留。
- 34 个录像数据库路径、23 张历史人脸裁剪图和 23 张历史场景图已完成 dry-run、应用和
  再次 dry-run；数量、总字节和内容摘要集合一致。
- 下载器补回 5 个遗漏录像后共有 39 条受管录像，全部处理完成且分析失败为 0；有效人脸
  裁剪图和场景图各 26 张，全部使用 `-aNNN` / `-bNNN` 新命名。
- 公网密码登录、安全 Cookie、新命名人物簇图片和源录像、CSRF 退出、会话撤销和共享
  网关全部既有路由通过。
- `V0.0.1` 公开 Release 源码包已完成 111 文件恢复验证；生产镜像、110 文件私有首版源码恢复集、模型私有恢复包、SHA-256 和数据库快照均保留并验证。
- NAS 生产容器健康；输入、模型和 Secret 只读挂载，根文件系统只读，资源上限为
  2 CPU、2 GiB、128 PIDs。
- V0.0.4 验收时 39 条受管录像全部处理；当时新增有效人物轨迹 3 条，分析错误 0。实际身份准确率
  仍需后续人工标记校准。
- 公网管理入口、密码登录、安全 Cookie、服务端会话、CSRF 退出及共享网关既有路由通过。
- 当前回滚保留 V0.0.5 镜像、源码、上线前数据库快照、私有配置与摘要清单；未删除模型、
  数据、Secret 或业务资源。
