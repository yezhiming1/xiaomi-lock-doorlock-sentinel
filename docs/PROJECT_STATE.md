# 项目状态

版本目标：`V0.0.6`
状态：`RELEASED / DEPLOYED / TRAINING`

## 已冻结

- NAS 独立运行，电脑不参与日常流程。
- 单镜像、单容器、Python/Node 两个独立受监督进程。
- 下载器独占小米云认证；识别服务只读本地录像与脱敏状态日志。
- SCRFD 2.5G + ArcFace R50 + ONNX Runtime CPU，单录像推理并发。
- 不预录人脸；自然学习永久持续。
- 多人同框逐人跟踪，同帧人物永久不可合并。
- 高质量、多样代表样本永久保存且总数不封顶。
- 身份与风险通知训练期关闭，运行故障通知开启。
- 普通本地制品保留 35 天；115 真实备份和删除归专项任务。
- 管理页面密码直登、12 小时可撤销服务端会话。
- 识别结果不得控制门锁。

## 当前实现

- `V0.0.6` 在既有关系分类中加入“我”和“朋友”，覆盖人物确认、修改、卡片、事件详情和
  合并选择器；历史人物不自动改写，名称留空时继续按关系编号。
- “我”与“家人”共享可信风险减分，“朋友”保持已知人物的中性风险语义；没有新增数据库
  字段或迁移。Python 50 项、Node 4 项及本地完整回归已通过。
- `V0.0.6` 的公开 Pull Request、CI、Release、NAS 隔离候选、上线前恢复集、无损切换和
  Edge 桌面/手机真实入口验收均已完成；生产容器健康、零重启。
- 应用源码、数据库迁移、管理页面、自然学习、Outbox、下载状态桥接和备份回执协议已完成。
- `V0.0.5` 将 API 时间规范为明确 UTC，管理页面固定按北京时间显示，并在页面标题、登录页
  和品牌区显示版本号；本地 Python 46 项、Node 4 项及完整回归已经通过。
- V0.0.5 生产镜像绑定合并提交 `442425065f845b3e7107bba09b7f67af4faa2d86`，部署后容器
  健康、重启计数为 0，原 `bridge` 网络、只读边界、资源上限和数据集合均保持不变。
- `V0.0.4` 增加北京时间录像名解析、`-aNNN` / `-bNNN` 派生图片命名，以及无覆盖、
  可重复 dry-run 的数据库与文件迁移。
- Python 43 项、Node 2 项、静态检查/构建/审计、迁移往返、合成烟雾链和模型锁已有通过证据。
- 下载器 `V0.0.8` 已部署到目标 Home Assistant；历史录像改名与 5 个遗漏录像补回后，
  最终 dry-run 为零待处理，脱敏状态桥接为健康。
- 生产容器已完成真实模型、健康、重启持久化、非 root 业务进程、`root:root 0640` 输入读取、只读根与只读输入验收。
- V0.0.4 以前代镜像和最终源码提交完成离线代码层镜像构建；迁移前在线快照独立校验通过，
  34 个数据库路径、23 张人脸裁剪图和 23 张场景图完成无覆盖迁移。
- V0.0.4 验收时 39 个受管录像全部处理，分析错误 0；有效人脸裁剪图与场景图各 26 张，全部
  使用新命名。数据库完整性正常，输入文件数量、总大小和内容集合摘要在迁移前后不变。
- V0.0.4 无损切换后生产容器健康、重启计数为 0，数据库聚合、只读挂载、资源上限与
  启动策略保持不变。
- 公网入口已通过 TLS、密码登录、安全 Cookie、新命名人物簇图片与源录像读取、会话和
  共享网关既有路由回归。
- 不公开人物名称、画面或真实时间线；自然学习与人工标记继续永久运行。

## 工作值

- Web 回环端口：`18125`。
- ONNX Runtime：intra-op `2`，inter-op `1`。
- 抽帧：`2 FPS`，每段最多 `120` 帧。
- 分析失败重试：`5/20/60` 分钟。
- 登录：15 分钟窗口内 5 次失败后锁定 30 分钟。
- 人物检索：历史原型不删除，最多启用 32 个多样检索原型及综合质心。

这些值是针对 N100 和当前短录像量的初始工作值，需以 NAS 私有性能证据复核；不能当成最终准确率阈值。

## 证据边界

- 本地/聚焦：`PASS`；V0.0.6 Python 50 项、Node 4 项、静态检查、构建、依赖审计、迁移
  往返、合成烟雾链、模型锁、公开树审计和 Compose 配置通过。
- 容器运行：`PASS`；V0.0.6 镜像、包版本和源码提交身份一致，真实模型就绪，生产容器
  健康、零重启，使用原 `bridge` 网络和既有资源上限。
- 浏览器：`PASS`；隔离候选与正式入口均通过 Edge 桌面和 390×844 验收，“我/朋友”在
  确认、修改、卡片、事件详情和合并选择器中显示正确，键盘顺序与触控目标通过。
- NAS 目标环境：`PASS`；上线前在线快照与 V0.0.5 恢复集完整，切换后数据库业务表、
  输入与派生文件集合一致，只读边界、资源上限和启动策略不变。
- 公网入口：`PASS`；TLS、现有登录会话、北京时间、版本号、真实事件与媒体消费者、
  无水平溢出和应用控制台零错误；共享入口未改动。
- 企业微信真实账号：`UNKNOWN`。
- 115 真实备份与恢复：本项目范围外，由专项任务验收。
- 实际身份准确率：`UNKNOWN`；必须由后续人工标记数据持续校准，不由本次部署推断。
