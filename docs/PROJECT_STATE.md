# 项目状态

版本目标：`V0.0.2`
状态：`RELEASED / DEPLOYED / TRAINING`

## 已冻结

- NAS 独立运行，电脑不参与日常流程。
- 单镜像、单容器、Python/Node 两个独立受监督进程。
- 下载器独占小米云认证；识别服务只读本地录像与脱敏状态日志。
- SCRFD 2.5G + ArcFace R50 + ONNX Runtime CPU，单录像推理并发。
- 不预录人脸；自然学习永久持续。
- 多人同框逐人跟踪，同帧人物永久不可合并。
- 高质量、多样代表样本永久保存且总数不封顶。
- 身份与风险通知训练期关闭，运行故障通知开启。
- 普通本地制品保留 35 天；115 真实备份和删除归专项任务。
- 管理页面密码直登、12 小时可撤销服务端会话。
- 识别结果不得控制门锁。

## 当前实现

- 应用源码、数据库迁移、管理页面、自然学习、Outbox、下载状态桥接和备份回执协议已完成。
- `V0.0.2` 增加空名称自动编号，以及人物簇大图、样本切换和对应录像播放。
- Python 36 项、Node 2 项、静态检查/构建/审计、迁移往返、合成烟雾链和模型锁已有通过证据。
- 下载器 `V0.0.7` 已部署到目标 Home Assistant；最终 dry-run 为零待处理，脱敏状态桥接为健康。
- 生产容器已完成真实模型、健康、重启持久化、非 root 业务进程、`root:root 0640` 输入读取、只读根与只读输入验收。
- V0.0.2 Edge 管理页面已完成桌面与 390×844 验收：人物簇大图、两张样本切换、录像完整播放、留空命名提示、键盘焦点、无水平溢出且控制台错误为 0。
- NAS 私有批次已全部处理：7 个事件完成识别流程、22 个因清晰度或无人脸跳过、分析错误 0；
  数据库完整性正常，输入文件数量、总大小和内容集合摘要在消费者复核前后不变。
- V0.0.2 已无损部署到 NAS；受控重启后容器健康，数据库聚合与源录像集合在升级前后不变。
- 公网入口已通过 TLS、密码登录、安全 Cookie、V0.0.2 管理接口、CSRF 退出、会话撤销和共享网关全部既有路由回归。
- 当前已有 3 个已确认人物和 6 个永久原型；不公开名称、画面或真实时间线，自然学习继续永久运行。

## 工作值

- Web 回环端口：`18125`。
- ONNX Runtime：intra-op `2`，inter-op `1`。
- 抽帧：`2 FPS`，每段最多 `120` 帧。
- 分析失败重试：`5/20/60` 分钟。
- 登录：15 分钟窗口内 5 次失败后锁定 30 分钟。
- 人物检索：历史原型不删除，最多启用 32 个多样检索原型及综合质心。

这些值是针对 N100 和当前短录像量的初始工作值，需以 NAS 私有性能证据复核；不能当成最终准确率阈值。

## 证据边界

- 本地/聚焦：`PASS`，见应用 `VALIDATION.md`。
- 容器运行：`PASS`，仅代表本机隔离候选环境。
- 浏览器：`PASS`，仅代表本机合成数据和 Edge 验收环境。
- NAS 目标环境：`PASS`；V0.0.2 健康、受控重启通过，数据库完整且源录像集合不变。
- 公网入口：`PASS`；TLS、密码登录、安全会话、V0.0.2 数据接口、CSRF 退出和共享网关回归通过。
- 企业微信真实账号：`UNKNOWN`。
- 115 真实备份与恢复：本项目范围外，由专项任务验收。
- 实际身份准确率：`UNKNOWN`；必须由后续人工标记数据持续校准，不由本次部署推断。
