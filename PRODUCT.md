# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

已委托选择：FastAPI 提供 JSON API 与静态 HTML/CSS/JavaScript 管理端。这样可在 NAS 的单一容器内运行，避免额外前端构建服务和常驻进程。

## Users

主要用户是家庭门锁的唯一管理员。用户会在手机或电脑上查看每天少量短录像，确认人物身份、修正聚类、查看失败任务和备份回执。界面必须使用非技术语言，不要求理解模型、数据库或容器。

## Product Purpose

门锁录像下载完成后，由 NAS 自动检测清晰人脸、形成持续学习的人物簇，并把事件、人物、失败原因和备份状态集中呈现。成功标准是：每天约五段、合计约三十秒的录像可在 NAS 上独立完成分析；人工确认可追溯、可撤销；失败不会静默丢失。

## Positioning

系统不要求预先录入人脸，而是从真实门锁事件中保守积累高质量代表样本；同框人物保持不可合并约束，人工操作可撤销，识别结果永远不参与开锁。

## Operating Context

- 上游下载器把已校验且原子落盘的录像写入 NAS 的门锁录像目录；本项目只读该目录。
- 识别、管理页面和企业微信机器人均在 NAS 的一个专用容器内运行。
- 身份和风险通知在训练期默认关闭；运行失败通知保持开启。
- 115 专项任务读取本系统产出的清单并回写备份回执；本系统不登录 115、不上传、不删除远端文件。
- 管理页面通过项目私有部署域名访问，公开资料只使用 `doorlock.example.invalid` 占位符；入口采用单密码登录。

## Capabilities and Constraints

- 人脸检测 SCRFD 2.5G；人脸特征 ArcFace R50；ONNX Runtime CPU；默认单并发。
- 不清晰人脸跳过并记录原因；多人同框独立跟踪，禁止同框轨迹自动合并。
- 支持人物命名、关系标记、合并、拆分、撤销、误检标记和失败任务重试。
- 普通录像及派生文件本地保留 35 天；高质量代表样本、人物/模型记录、向量和长期未知簇永久保留，数量不设硬上限。
- 所有敏感配置位于 Git 之外；公开仓库不包含真实录像、人脸、向量、凭据、私有路径、设备编号或运行数据库。
- 管理登录使用 Argon2id、12 小时可撤销服务端会话、Secure/HttpOnly/SameSite Cookie、CSRF、持久限速、可信主机与审计。

## Evidence on Hand

- NAS 上已有少量真实门锁录像，可用于部署后的私有目标环境验证，不进入仓库或报告。
- 既有下载器已经产生带 UTC 事件时间的稳定文件名。
- 旧参考包只作为实现前基线，不能作为正式版本或验收证据。
- 没有可公开发布的真实人物、录像、模型权重或业务指标；演示数据必须明确标记为合成数据。

## Product Principles

1. 隐私边界先于便利：识别服务不接触小米或 Home Assistant 凭据。
2. 保守学习优于强行命名：不清晰、低置信或同框冲突时保持未知。
3. 人工决定可追溯：每次命名、合并、拆分和撤销均有审计记录。
4. 失败可见且可恢复：下载、分析、通知和备份回执都不能静默失败。
5. NAS 独立运行：部署完成后，电脑不参与日常下载、推理或管理。

## Accessibility & Inclusion

管理端目标为 WCAG 2.1 AA：正文对比度至少 4.5:1，完整键盘操作，清晰焦点状态，44×44 像素触控目标，状态不只依赖颜色，并尊重减少动态效果偏好。
