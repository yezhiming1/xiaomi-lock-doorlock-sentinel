# 项目协作规则

## 修改前

1. 本目录是“小米门锁录像识别训练”的唯一项目根；所有项目源码、文档、测试、发布包和本地交接均留在本目录内。
2. 每次修改前读取 `docs/PROJECT_STATE.md`、`docs/HANDOFF.md`、`docs/CHANGELOG.md`、`docs/RELEASE.md` 和 `docs/PENDING.md`（存在时）。
3. GitHub 同步遵循全局规则：从干净且等于 `origin/main` 的 `main` 建立 `agent/<task>` 分支；不得覆盖、清理、重置或猜测合并不明改动。

## 不可变产品边界

- 本项目不连接小米云、不读取 Home Assistant、Xiaomi Miot、米家或下载器的密码、Cookie、令牌和认证文件。
- 已有下载器是唯一录像上游；本项目只读消费已经完整落盘的本地录像和无敏感信息的状态清单。
- 正式运行采用一个专用容器，容器内保留两个可独立监控的服务进程：`recognition` 与 `wecom-bot`。
- `recognition` 是业务数据库唯一写入者；`wecom-bot` 只通过认证的 loopback API、持久化 Outbox 和租约通信。
- 识别主线固定为 SCRFD + ArcFace + ONNX Runtime CPU；不同 `model_id` 的特征不得比较。
- 自然事件学习不要求专门录入人脸；同框不同人物必须保留不可合并约束；任何识别结果都不得触发自动开锁。
- 本项目不实现 115 登录、上传、月度封存、恢复缓存或本地清理。只输出制品清单并读取“网盘备份专项”回执。
- 录像输入挂载为只读；本项目不得删除、改名或覆盖源录像。

## 公共仓库安全边界

- 仓库公开。不得提交真实录像、人脸裁剪、特征向量、数据库、家庭事件时间、设备或录像标识、签名 URL、账号、密码、Cookie、Token、Secret、私有挂载路径、RFC1918 地址、本地主机名或真实部署域名。
- 模型权重不进入 Git；仓库只保存来源、许可证、文件名、大小和 SHA-256 清单。
- 测试只使用合成数据、生成视频和不可还原真实身份的固定向量。
- 运行时密钥必须通过只读 secret 文件挂载；日志只允许固定错误码、计数和去标识摘要。

## 版本、验证和发布

- 首个正式版本为 `V0.0.1`；旧 `doorlock-sentinel-v0.1.0.zip` 是未验收参考包，不是正式前代。
- Python 聚焦测试：`python -m pytest -q`；静态检查：`python -m ruff check .`。
- 企业微信服务：在 `services/wecom-bot` 中运行 `npm test` 和 `npm run build`。
- 发布前必须运行完整验证、公开树安全扫描、容器构建、数据库迁移往返、合成端到端测试、健康检查和管理页面多尺寸截图验收。
- 正式发布保留源码包、容器镜像归档、文件清单、SHA-256、部署状态、回滚步骤和真实消费者验证结果。
- NAS、共享公网网关和 GitHub 是不同信任边界；每次写入后必须从权威目标重新读取并验证。

## 文档语言与界面

- 项目管理、状态、交接、变更、发布和运维文档使用中文；代码标识、协议、命令和公共仓库名称保持英文。
- 管理页面面向非技术用户，默认中文，必须支持键盘、移动端、明确错误状态和 44px 以上触控目标。
